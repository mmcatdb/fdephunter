package de.uni.passau.core.graph;

import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.Test;

import java.util.List;

public class VertexTest {
    @Test
    public void testAddIncomingEdge() {
//        Vertex vertex = new Vertex("A");
//        Edge edge = new Edge(new Vertex("B"), vertex, 5.0);
//        List<Edge> incomingEdges = vertex.__getIncomingEdges();
//        System.out.println(incomingEdges);
//        Assertions.assertEquals(1, incomingEdges.size());
//        Assertions.assertEquals(edge, incomingEdges.get(0));
    }
    
    @Test
    public void testGetIncomingEdges() {
//        Vertex vertex = new Vertex("A");
//        Edge edge1 = new Edge(new Vertex("B"), vertex, 5.0);
//        Edge edge2 = new Edge(new Vertex("C"), vertex, 10.0);
//        List<Edge> incomingEdges = vertex.__getIncomingEdges();
//        Assertions.assertEquals(2, incomingEdges.size());
//        Assertions.assertEquals(edge1, incomingEdges.get(0));
//        Assertions.assertEquals(edge2, incomingEdges.get(1));
    }

}
